@model List<Artista>


@{
    Layout = "~/Views/Shared/_Layout.cshtml";

}
@section Styles {
    <link rel="stylesheet" href="~/css/cards.css" asp-append-version="true" />
}

<h1>Lista de artistas:</h1>

@* 
@foreach (Artista a in Model)
{
    <div class="card" style="width: 18rem; float: left; margin-right: 15px;">
        <img class="card-img-top" src="~/images/artistas/@a.Imagen" alt="Card image cap">
        <div class="card-body">
            <h5 class="card-title">@a.Nombre</h5>
            <a asp-controller="Home" asp-action="DetallesArtista"
               asp-route-idartista="@a.IdArtista"
               class="btn btn-primary">Ver detalles!</a>
        </div>
    </div>
} *@


<div id="carouselExampleControls" class="carousel">
    <div class="carousel-inner">
        @foreach (Artista a in Model)
        {
            <div class="carousel-item ">
                    <a asp-controller="Home" asp-action="DetallesArtista"
                       asp-route-idartista="@a.IdArtista"
                   style="color: black;text-decoration: none;">
                    <div class="card">
                        <div class="img-wrapper">
                            <img src="~/images/artistas/@a.Imagen" class="d-block w-100 "
                                 alt="@a.Nombre" style="border-top-left-radius:10px;border-top-right-radius:10px;">
                        </div>
                        <div class="card-body">
                            <h5 class="text-white">@a.Nombre</h5>
                            
                            @* <a asp-controller="Home" asp-action="DetallesConcierto"
                        asp-route-idevento="@e.IdConcierto"
                        class="btn btn-primary">Ver detalles!</a> *@
                        </div>
                    </div>
                </a>
            </div>
        }
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleControls" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>
